---
// Superseded by the all-portal acquirer scorecard — keep old bookmarks working.
export const prerender = false;
const t = Astro.url.searchParams.get('t');
return Astro.redirect(`/admin/acquirers${t ? `?t=${t}` : ''}`, 301);
---
